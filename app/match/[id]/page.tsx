import { PagePlaceholder } from "@/components/PagePlaceholder";

// Le détail du match (avec pronostic) est implémenté à l'étape 5.
export default function MatchPage() {
  return <PagePlaceholder title="Détail du match" step="étape 5" />;
}
